# Core ML / NLP
sentence-transformers>=2.2.0
scikit-learn>=1.1.0
numpy>=1.24.0,<2.0
transformers>=4.21.0

# Web UI
streamlit>=1.32.0

# Language detection (for translator.py — optional for English-only mode)
fasttext-wheel>=0.9.2

# Downloader utility
requests>=2.0.0

# Optional spaCy (loaded in ipc.py legacy — not used in ipc_core.py)
# spacy>=3.4.0

# Note: run separately after install:
# python -m spacy download en_core_web_sm  (only if using legacy ipc.py)
